import Layout from "@components/Layout";
import { UserClient } from "@components/user/UserClient";

const Client = () => {
    return (
        <Layout title="User Tasker">
            <UserClient />
        </Layout>
    );
};
export default Client;
