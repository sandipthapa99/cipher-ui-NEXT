import { TaskCardProps } from "types/taskCard";

const TaskCard = ({
    title,
    charge,
    description,
    location,
    date,
    time,

}: TaskCardProps) => {
    return(
        <div className="task-card-block">            
        </div>
    )
}
export default TaskCard;