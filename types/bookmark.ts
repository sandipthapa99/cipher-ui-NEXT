export interface BookmarkValueProps {
    id: string;
    type: string;
}

export interface BookmarkResult {
    id: number;
    model: string;
    created_at: string;
    updated_at: string;
    object_id: string;
}
